<!-- <p id="notice"><%= notice %></p> --> 

<% if user_signed_in? %>
<h1><strong>Friends List</strong></h1>
<br>

<table class="table is-bordered is-hoverable is-striped">
  <thead class="">
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Country</th>
      <th>Twitter</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @friends.each do |friend| %>
     <% if friend.user == current_user %>
      <tr>
        <td><%= link_to friend.first_name + " " + friend.last_name, friend %>
        <td><%= friend.email %></td>
        <td><%= friend.country %></td>
        <td><%= friend.twitter %></td>
<!--         <td><%= friend.user_id %></td>
 -->    <td><%= link_to 'Delete', friend, method: :delete, data: { confirm: 'Are you sure?' } , class: "button is-danger" %></td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Friend', new_friend_path, class: "button is-primary" %>

<% else %>

<h1>Hello Dude !!</h1>

Ruby and Rails is AWESOME 🚀

<% end %>
